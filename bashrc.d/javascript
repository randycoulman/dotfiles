if [ -x $(which yarn) ]; then
  export PATH=$PATH:$(yarn global bin)
fi

alias ya="yarn add"
alias yad="yarn add --dev"
alias yl="yarn lint"
alias ys="yarn start"
alias yt="yarn test"
alias ytw="yarn test:watch"
